name: Sync Remote Branch

on:
  workflow_call:
    inputs:
      version:
        description: 'The version string (e.g., 1.20.1)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  sync-remote-branch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository (full clone)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Set up git user
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Delete local release/<version> branch if exists
        run: |
          git branch -D release/${{ github.event.inputs.version }} || true

      - name: Delete release release/<version> branch if exists
        run: |
          git push origin --delete release/${{ github.event.inputs.version }} || true

      - name: Create new release/<version> branch from version/<version>
        run: |
          git checkout version/${{ github.event.inputs.version }}
          git checkout -b release/${{ github.event.inputs.version }}

      - name: Force push release/<version> branch
        run: |
          git push origin release/${{ github.event.inputs.version }} --force
